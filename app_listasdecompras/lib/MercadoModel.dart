import 'package:flutter/material.dart';

class Produtos {
  String descricao;
  String tipo;
  bool concluida;
  DateTime dataHora;

  Produtos(this.descricao, this.tipo, this.concluida, this.dataHora);
}
